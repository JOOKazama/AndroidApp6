<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          xmlns:tools="http://schemas.android.com/tools"
          package="com.RecyclerViewLogic">

    <application
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            tools:ignore="AllowBackup"
            android:theme="@style/AppTheme">

        <activity android:name="com._Main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity android:name="com._Operations.Remove" android:theme="@style/Pop_up"/>
        <activity android:name="com._Operations.Add" android:theme="@style/Pop_up"/>
    </application>

</manifest>